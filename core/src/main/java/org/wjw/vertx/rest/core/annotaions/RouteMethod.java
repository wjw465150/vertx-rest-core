package org.wjw.vertx.rest.core.annotaions;

/**
 * Router API 请求处理方式枚举.
 */
public enum RouteMethod {

  /** The options. */
  OPTIONS,
  /** The get. */
  GET,
  /** The head. */
  HEAD,
  /** The post. */
  POST,
  /** The put. */
  PUT,
  /** The delete. */
  DELETE,
  /** The trace. */
  TRACE,
  /** The connect. */
  CONNECT,
  /** The patch. */
  PATCH,
  /** The route. */
  ROUTE
}
